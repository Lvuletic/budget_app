nelmio_api_doc:
    documentation:
        servers:
            - url: http://budget-app.test
              description: API over HTTP
        info:
            title: Budget app
            description: Home budget app
            version: 1.0.0
    areas:
        path_patterns: # an array of regexps (document only routes under /api, except /api/doc)
            - ^/api(?!/doc$)
